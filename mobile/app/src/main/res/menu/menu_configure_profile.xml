<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/configure_profile_action_save"
        android:title="@string/configure_profile_save"
        app:showAsAction="always"/>

    <item
        android:id="@+id/configure_profile_action_cancel"
        android:visible="false"
        android:title="@string/configure_profile_cancel"
        app:showAsAction="never"/>

    <item
        android:id="@+id/configure_profile_action_logout"
        android:title="@string/text_logout"
        app:showAsAction="never"/>
</menu>